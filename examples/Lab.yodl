
module Top(
    clk: uint<1>,
    rst: bool,
) -> (
    is_active_area: bool,
    screen_x: uint<10>,
    screen_y: uint<10>,
    r: uint<3>,
    g: uint<3>,
    b: uint<3>,
) {
    let color = Reg<{ r: uint<3>, g: uint<3>, b: uint<3> }>(clk, rst);
    let c = color.q;
}
