import VGA

module TopSim(
    clk: logic,
    rst: logic,
) -> (
    is_active_area: logic,
    screen_x: logic[10],
    screen_y: logic[10],
    r: logic[3],
    g: logic[3],
    b: logic[3],
) {
    let vga = VGA::SyncPulses(pixel_clk: clk, col: screen_x, row: screen_y);
    let color = if vga.is_active_area {
        { r: 3'd3, g: 3'd5, b: 3'd7 };
    } else {
        { r: 3'd0, g: 3'd0, b: 3'd0 };
    };

    is_active_area = vga.is_active_area;
    r = color.r;
    g = color.g;
    b = color.b;
}
