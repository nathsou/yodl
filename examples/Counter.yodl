
module Top(
    clk: uitn<1>,
    sw: uint<16>,
    btn_c: bool,
) -> (
    led: uint<16>,
) {
    let counter = Reg<32>(clk, rst: btn_c);
    counter.d = counter.q + sw;
    led = counter.q[31-:16];
}
