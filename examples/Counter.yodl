
module Top(
    clk: logic,
    sw: logic[16],
    btn_c: logic,
) -> (
    led: logic[16],
) {
    let counter = Reg<32>(clk, rst: btn_c);
    counter.d = counter.q + sw;
    led = counter.q[31:16];
}
