# Hello

In the previous example, we saw how to generate correctly timed signals for a VGA interface. In this example, we will display some text on the screen.

```yodl
{{#include ../../../examples/lib/Text.yodl}}
```

Now we can use modules from the `Text` package to display a simple some scrolling messages on the screen with a random colour.

```yodl
{{#include ../../../examples/Hello.yodl}}
```
